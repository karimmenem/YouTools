// This file is intentionally left blank.
// Brand data is now managed through the admin panel and stored in localStorage.
//
// Static brands dataset used for production seeding.
// Populate this array by exporting your current local brands from the browser console:
// copy(JSON.parse(localStorage.getItem('brands')||'[]'))
// Paste the resulting array below (ensure each brand has id, name, slug, logo fields).
export const brands = [
  // Example:
  // { id: 1, name: 'ACME', slug: 'acme', logo: 'data:image/webp;base64,....' }
];

