// Static posters dataset used for seeding Mirage in production.
// Replace the sample array with your exported posters.
// Each poster: { id: <number>, image_url: <dataUrl or public path>, [other fields...] }
export const posters = [
  // Example:
  // { id: 1, image_url: 'data:image/webp;base64,XXXX...' }
];
